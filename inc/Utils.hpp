# pragma once

#include <iostream>
#include <map>
#include <string>

#include <ctime>
#include <sstream>

std::map<std::string, std::string>	getMimeTypes(void);
std::string							getFormattedTime(void);